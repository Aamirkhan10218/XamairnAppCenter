<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamairnAppCenter.Views.AboutPage"
             xmlns:vm="clr-namespace:XamairnAppCenter.ViewModels"
             Title="{Binding Title}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Text="Download DBR" Clicked="Button_Clicked"></Button>
   
        <WebView Grid.Row="1" x:Name="api" VerticalOptions="Fill" HorizontalOptions="Fill"></WebView>
    </Grid>

</ContentPage>
